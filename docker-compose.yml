version: '3.8'

services:
  esg-risk-map:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    restart: unless-stopped
